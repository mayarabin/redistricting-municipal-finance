# Separated Cleaned Data

This folder contains cleaned data files that remain separated by source, year, state, category, or stage of the data construction process.

The full combined cleaned dataset is stored directly in the parent `cleaned-data` folder when it is available for public sharing.
